# Log

Append-only timeline. One line per operation: `## [YYYY-MM-DD] operation | description`.
Grep-able. The wiki *pages* hold current state; this file holds the sequence of how it
got there.

## [2026-06-17] fix | Guidepour de-fiction (final model, superseding the facet idea below) + connector-line DRY. (1) The mistaken `Fiction` tag was traced to commit `d86cdbf` ("added Fiction where missing"); the 8 real-life-fact pairs are those that LACKED Fiction before it (agnostic, goldfish, sleeper, deja-vu, second, hasty-generalization, mathematician, magic + FR twins). A one-off removed their `Fiction` tag (facets:[fiction] kept — Martin's call: they stay Djosh Sho column pieces under the Book door). (2) `migrate_tags.plan_pair` now NEVER pair-unions `Fiction` across a guidepour twin (so it can't creep back), replacing the short-lived facet-driven rule. (3) Site: the constellation geometry (`rectEdge`/`circEdge`/`orbitPoint`) was extracted to `src/lib/constellation-geometry.ts` and is now imported by BOTH `Constellation.astro` and `StarMap.astro` (no more duplicated maths); the pill-corner gap fix lives there. (4) `/guidepour` list shows "Guide pour *title*" / "Guidance for *title*". 163 tests green; site builds (285 pages); migrate dry-run 0. Nothing committed.

## [2026-06-17] fix | `migrate_tags.py`: the `Fiction` tag now follows the `fiction` **facet** for guidepour (`Djosh Sho`) posts — some "Guide pour" entries are real-life facts, not fiction, and were wrongly tagged. `plan_pair` takes the pair's facets; if a Djosh Sho pair lacks the fiction facet, the Fiction tag is dropped. So the operator de-fictions an entry by removing its (pair-shared) fiction facet, re-running drops the tag, and it still lists on `/guidepour` (the page now filters by the Djosh Sho tag only, not the fiction facet — site change). HEAD already had the fiction facet on all 32 Djosh Sho posts, so nothing changes until facets are corrected. 163 tests green. See [[tag-vocabulary]].

## [2026-06-17] fix | `migrate_tags.py` no longer forces `Gamsblurb` onto **quote** posts — a Quote/Citation post isn't a Gamsblurb (Martin, after running the script: "it's a quote, it's not a gamsblurb"). Confirmed against HEAD: the only posts ever missing the house tag were the quotes. Added `HOUSE_EXCLUSIVE = {"Quote"}`; `plan_pair` withholds (and strips) the house tag when either twin carries the Quote concept, so a re-run cleans up the wrongly-added tags. Dry-run after Martin's run: 10 quote files (the Quote/Citation pairs) would revert; everything else idempotent. 161 tests green. See [[tag-vocabulary]].

## [2026-06-17] ingest | Built the controlled bilingual tag system ([[tag-vocabulary]]). New `tag_vocab.py` (≈100 `{en,fr,aliases}` concepts; casefold folding of casing/typo/synonym/leak variants). `article.py` `set_tags` now mirrors vocabulary tags onto the FR/EN twin like `set_facets` ([[bilingual-pairing]]); `articles.py` `translate` maps known tags deterministically through the vocab (Google only for one-offs). New `tag_index.py` (cached freq + facet co-occurrence) feeds the picker via `webapi.tag_suggestions`/`toggle_tag` ([[webapi-bridge]]); `web/js/meta.js` renders `matching`+`popular` chip rows under the Tags input (verified in the dev harness). New `migrate_tags.py` normalizes + pair-unions tags across the site twins, rewriting ONLY the `tags:` line — dry-run showed 85/263 files; **not applied** (site repo had unrelated uncommitted work). 160 tests green. Site-side consumers (related-constellation, tag-galaxy) live in the martingamsby.com repo. Nothing committed.

## [2026-06-17] ingest | **Instagram publishing built** ([[instagram-adapter]], [[publishing]], [[social-publishing]], [[link-slots]], [[post-base]], [[facebook-adapter]], [[webapi-bridge]], [[web-ui]], [[secrets]], [[glossary]]). New `post_ig.PostIG` (inherits [[post-base]], reuses `settings_fb_<hl>.ini` + an `IgUserId` field, shares `post_fb.GRAPH_API_VERSION`): `post(msg, …, image_url=)` does the Graph Content-Publishing dance — `POST /{ig-user}/media` (image_url+caption) → container, `…/media_publish` → media id, `GET /{media}?fields=permalink` → the post URL; raises on a Graph error / unreadable permalink (names the media id so the operator sets the link by hand). IG fetches the image **server-side**, so it needs a PUBLIC URL: new `site_push.py` copies the grabbed JPEG into the site repo's `public/assets/ig/<slug>.<hl>.jpg`, git-**auto-pushes** just that file, and builds the `raw.githubusercontent.com` URL from `origin` (owner/repo) + branch (no new config, no deploy). Registered `PLATFORMS["instagram"]` (max 2200, image-only); `publish` intercepts IG → `_publish_instagram` (needs `opts["image_url"]`); `stage_instagram_image` is step 1 (`localize.find_repo_root` → `site_push`). Bridge: `webapi.publish_instagram_image` + the `Instagram` LINK_SLOT (`publish:"instagram"`, both langs). UI: `js/publish.js` renders an image-only **two-step** panel (caption ≤2200 → **1·Push image** with a streamed git log → **2·Publish**), per the user's "make it a clear step with feedback" call. The auto-push (the one place WriterHelper writes to git) was the chosen approach over the wiki's old "operator pushes by hand"; FB cross-post stays a myth → post both directly. Verified end-to-end in the dev harness (push log, URL, publish success, facet + no-card guards). Tests: `tests/test_post_ig.py`, `tests/test_site_push.py`, IG cases in `tests/test_publishing.py` (`fake_ig`). 121 pass.

## [2026-06-17] change | Instagram popup shows the **actual repo image** + repo setup steps documented ([[instagram-adapter]], [[publishing]]). `prepare_post` returns `ig_repo_image_data_url` (the image currently committed at `public/assets/ig/<slug>.<hl>.jpg`, as a data URL via `rendering.data_url`); `renderInstagram` renders it under step 1 with a state-aware caption — green "✓ in the repo now" when pushed, or a warning when it **differs from the current card** (operator re-grabbed) / isn't pushed yet (JS compares the two data URLs). Lets the operator eyeball that IG will fetch the right image. Also expanded [[instagram-adapter]] Config into standalone numbered setup steps (Graph API Explorer → permissions, long-lived token, `PageId`/`Token` off `/me/accounts`, `IgUserId` off `/{page-id}?fields=instagram_business_account`, fill both `settings_fb_<hl>.ini`). Test: `test_prepare_instagram_includes_repo_image_data_url`. Verified in the harness (pushed → "✓ in the repo", differing image → warning). 128 pass.

## [2026-06-17] fix | Instagram popup no longer forces a redundant re-push on reopen ([[instagram-adapter]], [[publishing]]). Cancel → reopen reset `igImageUrl` and re-locked step 2, so the operator had to push the (already-committed) image again. New read-only `site_push.image_status(repo, dest, local)` reports `{pushed, public_url}` — true only when the dest file exists, is **byte-identical** to the current grabbed card, and is tracked + committed + not ahead of origin (so a re-grabbed/changed card still needs a push). `prepare_post` surfaces `ig_image_pushed`/`ig_public_url`; `renderInstagram` pre-fills the URL + jumps to step 2 ("✓ Already pushed"). Tests: `image_status` cases in `test_site_push.py` (identical/missing/differ/unpushed/untracked) + `test_prepare_instagram_reports_already_pushed_image`. Verified in the dev harness (open→push→cancel→reopen leaves Publish enabled). 127 pass.

## [2026-06-16] fix | Prev/next navigation no longer gets stuck on a mixed-case filename ([[article-model]]). `get_slug()` lowercases the title, but `2024-09-14-a-PhD-in-your-pocket.md` keeps mixed case on disk; `open_adjacent_article` reconstructed the lowercased name and matched it `f == last_filename` (case-sensitive), so on case-insensitive Windows `os.path.isfile` passed but the in-loop match never did → couldn't step to the neighbour (EN-only, since the FR twin's slug was all-lowercase). Rewritten to find the current post in the **sorted** `.md` listing case-insensitively, then step ±1 (also filters non-`.md` like `open_last_article`). Test: `test_next_navigation_is_case_insensitive`.

## [2026-06-16] fix | Footer link slots no longer silently dropped on load ([[serializers]], [[link-slots]]). The old `_LINK_RE` required `http[s]?://` and matched a single line, so a footer entry like a legacy Bluesky slot holding the *post text* (multi-line, not a URL) was dropped on load and deleted on the next auto-save (`burnout.md`). Replaced with `_iter_footer_links`: split the footer at each `- [`, parse each entry to its final `)` (target may be non-URL / multi-line; a `)` inside a Wikipedia-style URL stays intact). Tests: `test_multiline_footer_link_is_preserved`, `test_footer_url_with_parens_round_trips`.

## [2026-06-16] fix | Frontmatter `title:` is now escaped as a YAML double-quoted scalar ([[serializers]], [[astro-format]]). `serialize` emitted `title: "%s" % article.title` raw, so a quoted title (`I just watched "the Martian" …`) produced invalid YAML; worse, loading a correctly-escaped title and re-saving (every save) re-broke it. New `_yaml_dq` escapes `\` then `"`; the round-trip is preserved. Test: `test_title_with_quotes_is_escaped_and_round_trips`.

## [2026-06-16] change | Optional **Bluesky link-preview card** (external embed), with YouTube support ([[bluesky-adapter]], [[publishing]], [[social-publishing]], [[link-slots]], [[post-base]]). New `post_bsky.fetch_external_card(client,url)` scrapes OpenGraph title/desc/image, uploads the thumb (`upload_blob().blob`, a real `BlobRef`), returns `app.bsky.embed.external`; YouTube links (`youtube_id`) skip scraping and use `img.youtube.com/vi/<id>/hqdefault.jpg` so Bluesky shows a playable card; any failure → None (post plain, never block). A post's embed slot is image-OR-external, so the card rides a no-image post (thread: first post only). `embed_url` threaded through `post`/`post_thread` (base/PostX/PostFB accept+ignore — they auto-unfurl). `publishing.embed_candidate(article,msg)` picks the URL (YouTube slot > first URL in text); `prepare_post` returns `embed_url` (Bluesky only); `publish`/`_publish_thread` honour `options["embed"]`. UI: opt-in-by-default checkbox in text/thread modes (hidden in image mode) + mock card in the preview dock. Verified in the dev harness (checkbox + options `{embed}` for text/thread, hidden for image). Tests: `test_post_bsky.py` (youtube_id, embed build/youtube-thumb/fail-soft/missing-thumb), `test_publishing.py` (embed_candidate priority, prepare/publish wiring, non-bsky ignores).

## [2026-06-16] fix | Date-dependent golden test `test_astro_serialize_golden` pinned. translationKey mints from the EN twin's date; the test set only the FR date, so the key drifted to today (06-16 ≠ hard-coded 06-13). Now pins `fr.ref.set_date(...)` too — deterministic regardless of run date.

## [2026-06-16] change | Bluesky posts now carry **clickable link facets** ([[bluesky-adapter]]). URLs in a post were plain un-clickable text; `_send` now runs the message through new module-level `build_rich_text(msg)` (so single posts AND threads benefit). No URL → returns the plain `str` unchanged; one+ URLs → an `atproto.client_utils.TextBuilder` with a `.link(url,url)` facet per URL (`send_post`/`send_image` accept `str | TextBuilder`). `_trim_url` strips trailing sentence punctuation and an *unbalanced* `)]}` (so `…/Foo_(bar)` survives); facet ranges are UTF-8 byte offsets so FR accents before a URL stay aligned. Tests: `tests/test_post_bsky.py`.

## [2026-06-16] fix | A dropped image whose self-host hook failed transiently left a 100KB+ `data:` blob persisted in `fr/2026-06-15-ok.md`. `_localize_excerpt_image` now retries once on a `data:` value and, if still failing, drops the image + re-saves rather than persist the blob (new invariant, [[invariants-and-traps]] / [[astro-format]]). Recovered the affected FR post by running the site hook. Tests in `test_article.py`.

## [2026-06-15] fix | `rendering.plain_text` now separates paragraphs with a blank line ([[rendering-module]], [[social-publishing]]). `BeautifulSoup.get_text()` was dropping the `<br/>`s, so paragraphs ran together on one line — visible on Facebook (renders the message text literally; the user was adding the breaks by hand). Fix: convert `<br/>` → newline before stripping, trim each line, collapse 3+ newlines → one blank line. Improves all platforms; the thread splitter already prefers `\n\n` paragraph boundaries (`thread_split._chunk`). Test: `test_plain_text_separates_paragraphs_with_blank_line`. Also recorded the durable constraint in [[facebook-adapter]]: **the Graph API can't post to a personal profile** (Meta removed it in 2018) — Pages only.
## [2026-06-15] change | Captures are now slug-named JPEGs — the "right article" guarantee ([[image-card-capture]], [[invariants-and-traps]], [[social-publishing]], [[instagram-adapter]]). Was `richTextArea_<hl><N>.png` (keyed only by language → a stale grab from another article could be published/committed). Now `richTextArea_<slug>_<hl><N>.jpg`, built by the single source of truth `publishing.capture_filename(article, page)` (`image_filename` = page 1): `capture.js` emits JPEG (`toDataURL('image/jpeg',0.92)`, white bg) and `webapi.save_capture` derives the name from the current article's slug; publishing resolves the *current* slug, so a different article's grab simply isn't found (editing the title → new slug → re-grab). JPEG so the same file is the Instagram asset (no conversion). `.gitignore` `richTextArea*`. dev-mock updated. Tests: slug-name, producer↔consumer loop (`save_capture` writes what `image_filename` expects), and a by-slug resolution test; existing image/thread tests made scheme-agnostic via `publishing.image_filename`. (FB config also moved `fb_settings_` → `settings_fb_<hl>.ini`.)
## [2026-06-15] ingest | [[instagram-adapter]] planned page + FB token how-to. Captured the **IG-must-be-last** ordering disclaimer (render JPEG → push image → IG container+publish → write IG link into article → push article; forced because IG fetches a public image URL server-side and the post URL only exists after publishing). Two hard constraints: (1) IG accepts **JPEG only** — the PNG grab card and WebP self-hosted image are both invalid, so the square card must be converted to JPEG; (2) the public URL can be a **`raw.githubusercontent.com` URL** (no site build/deploy needed, repo is public). Added the non-expiring Page-token steps to [[facebook-adapter]] (Graph API Explorer → exchange short→long user token → read Page token off `/me/accounts`; works in app Development mode, no App Review for your own Page).
## [2026-06-15] ingest | Facebook publishing wired ([[facebook-adapter]], [[publishing]], [[social-publishing]], [[link-slots]]). `post_fb.PostFB` rewritten onto [[post-base]]: `post(msg,image,alt)` → `POST /{page}/photos` (local file as `source` multipart + `alt_text_custom`, URL from `post_id`) or `/{page}/feed`, returns `https://www.facebook.com/<id>`, raises `RuntimeError` on a Graph `error` so the popup shows it. Graph version in `GRAPH_API_VERSION` (`v21.0`). Fixed the `__init__` bug that assigned `config['Access']` twice and dropped `Token`. Kept `settings_fb_<hl>.ini`. Registered `PLATFORMS["facebook"]` (max 63206, no `post_thread` → text/image only); Facebook LINK_SLOT now `publish:"facebook"` on both langs. Token/permission setup (long-lived Page token + `pages_manage_posts`) is the real "didn't work" cause, not code. Tests: `tests/test_post_fb.py` (requests mocked) + `fake_fb` cases in `test_publishing.py`. Next-session note: Instagram rides the same Meta token but needs a public image URL — decided to push the square card to the Astro site first.
## [2026-06-15] ingest | [[x-adapter]]: X killed the Free API tier (2026). Un-enrolled apps fail v2 `create_tweet` with 403 `client-not-enrolled` (echoes the app `client_id`) — portal state, not code/creds. Fix: console.x.com → Apps → "move this app" to Pay-Per-Use production (~$0.015/post, ~$0.20 w/ URL). Enrollment is per-app. Also documented: `handle` in the ini is cosmetic (URL only); the posting account = the access token, so en=@Martin_Gamsby / fr=@MartinGamsby can't be swapped by changing the handle.
## [2026-06-15] update | [[publishing]] + [[social-publishing]]: publish failures now reach the UI instead of hanging. `js/publish.js` `send()` wraps `API.publish` in try/catch so a rejected bridge promise (Python raised) restores the popup + shows the error. `publishing.publish`/`_publish_thread` wrap the adapter call: `_post_error` maps the exception to a short popup message (special-cases the X 403), `_log_post_exc` prints full stack + tweepy `api_codes`/HTTP body to stderr.
## [2026-06-15] ingest | Thread image-source choice ([[social-publishing]]): the "Attach image to first post" control now picks between the **article image** (the post's own header, the new default so a thread shows the real picture) and the **grabbed text card**. `options.image` went bool → `"none"|"grabbed"|"article"`; `publishing._resolve_image` resolves the path (article via new `rendering.excerpt_image_file`, factored out of `excerpt_image_local`); grabbed alt = full text, article alt = title. `prepare_post` adds `article_image_exists`/`article_image_data_url`; `js/publish.js` adds the source radios (`threadImageSource`/`threadImageData`) + preview wiring.

## [2026-06-14] ingest | Publish popup gained a live WYSIWYG preview dock ([[social-publishing]]): two-column modal — controls left, "what you'll post" cards right (`renderPreview`/`postCard`/`numberedSegments` in `js/publish.js`). Text → one card; image → title + PNG (image moved OUT of the controls into the preview); thread → segments rendered exactly as sent (numbered, connector-joined cards, image on first). `prepare_post` adds `author` (`martingamsby.com/<hl>`, a loose preview label, not the real handle). New CSS `.pub-cols`/`.pv-*`.

## [2026-06-14] ingest | Thread mode: publish a too-long article as an editable X/Bluesky reply chain ([[social-publishing]], [[thread-split]]). New pure `thread_split.py` (paragraph→sentence→word→hard split; numbering reserves ` (i/n)` budget; `---`-line separators round-trip). `prepare_post` now seeds `thread_text`/`thread_count`/`separator` and suggests `thread` (was `image`) when text doesn't fit; `publish` gained `mode=="thread"` + `options={number,image}` → `_publish_thread`. Adapters got reply-chaining `post_thread` (Bluesky `ReplyRef`+`create_strong_ref`; X `in_reply_to_tweet_id`), base raises `NotImplementedError`; image attaches to first post only, first URL is the slot guard. `js/publish.js` adds the 3rd radio + editable segments + per-segment counts (mirrors the splitter); `webapi.publish` passes `options`. Tests: `test_thread_split.py`, thread cases in `test_publishing.py`. (Pre-existing date-golden test `test_astro_serialize_golden` fails only because today is the 14th — unrelated.)

## [2026-06-14] fix | "New (both) articles" silently deleted the article you just left ([[auto-save-and-pull-ui]], [[file-storage]]): a blank `new_article` skips `updated()` on its empty slug, so `ContentFile.last_filename` still pointed at the previous real post while `delete_last` was True — the first keystroke of the new title rename-by-deleted the old file. Fix: `new_article` now resets `self.content_file.last_filename = ""` (a new post is not a rename of the one before it). Regression tests: `test_new_article_does_not_delete_previous_post`, `test_new_both_articles_does_not_delete_previous_posts`.

## [2026-06-14] ingest | Drop/Browse a local image for an article ([[astro-format]], [[web-ui]], [[webapi-bridge]]): meta panel gains a drop zone + Browse… button. Both convert the file to a `data:` URL and feed `set_excerpt_img`, reusing the existing self-hosting hook unchanged (data: was already first-class in `is_remote`/`isExternal`/Node fetch) → slug-named webp pair. New: `localize.file_to_data_url`, `webapi.open_image`, `Meta.wireImage`, app.js stray-drop guard. No change to the sibling site repo.

## [2026-06-14] fix | Card image-preview broken after the Astro migration ([[astro-format]]): `rendering.excerpt_image_local` resolved `/assets/posts/<slug>.header.webp` against `posts_folder.parent` (= `…/blog`) but the webp lives under the site's `public/`. Now walks up to the site root via `localize.find_repo_root` (renamed from `_find_repo_root`) then into `public/`, with absolute + legacy-blog-relative fallbacks. Also consolidated `rendering.data_url` onto `localize.file_to_data_url` so webp gets a real `image/webp` MIME (Windows mimetypes lacks it; `<img>` won't render `application/octet-stream`).

## [2026-06-13] migrate | Converted the old `memory/` Memory system to the Karpathy-style `wiki/`. Ported every memory file (incl. the `startup-freeze` + `translationkey-bug` additions) into [[overview]], [[glossary]], concepts/, architecture/, sources/; rewrote `CLAUDE.md` to point at the wiki; added the `llm-wiki` skill (`.claude/skills/llm-wiki/`); repointed `.gitignore` (`wiki/tmp/`). No code behavior changed.

## [2026-06-13] ingest | Domain cutover for [[martingamsby-site]]: site live at apex `martingamsby.com`; WriterHelper's social/"Based on" URLs share the Astro base `https://martingamsby.com/<hl>/blog/`.

## [earlier] ingest | Hard switch to the Astro write format ([[astro-format]]): `serializers.py` became the format seam; `settings_<hl>.ini` repointed at `martingamsby.com/src/content/blog/<hl>`; Jekyll became load-only ([[jekyll-format]]).

## [earlier] ingest | Self-hosted preview images: remote `excerpt_image` URLs are downloaded + re-encoded to webp by the site's `tools/localize-images.mjs`, invoked via [[serializers]]'s `localize.py` hook. Frontmatter gains `image:` + `imageThumb:`.

## [earlier] refactor | Split the monolithic `model.py` into the Qt-free [[model-layer]] ([[article-model]], [[articles-model]], [[rendering-module]], [[publishing]]); built the pywebview [[web-ui]]. `model.py` + `backend.py` are now legacy ([[legacy-qt-ui]]).

## [earlier] ingest | Publishing became two-phase behind a confirmation popup ([[social-publishing]]): `prepare_post` (no side effects) + `publish`.

## [earlier] security | LinkedIn credentials that had been duplicated into a memory file were scrubbed from git history; `post_linkedin.py` values redacted in the working tree. The adapter stays broken/unused. See [[linkedin-adapter]], [[secrets]].
